# Campaign Notes Moved

Campaign trackers now live in the Obsidian vault:

- `note/EoABR-vault/experiments/campaigns/`
- `note/EoABR-vault/experiments/experiments_tracker.md`

Run experiment scripts from this code checkout as usual. `experiments/update_global_tracker.py` and `experiments/update_series_tracker.py` write Markdown to the note vault by default when the workspace layout is available.
